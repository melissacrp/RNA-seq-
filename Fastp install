conda install -c bioconda fastp
for x in *.fastq; do fastp -i $x -o "out-$x.gz"; mv "out-$x" /Volumes/MelissaCRP/Transcriptomics-RNA-seq/fastpfilter; done
#in line 2 i haven't figured out how to move all the files that are generated (.html, .json and fastq) into a new folder inside the directory fastpfilter
#when i perform this code the .html and .json files overwrites into the previous ones generated by the loop
